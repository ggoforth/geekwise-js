/*global $*/
(function () {

    "use strict";

    //load the navigation into the site so I don't have to update a million pages
    //just to update some links
    $("header#nav").load("nav.html");

}());